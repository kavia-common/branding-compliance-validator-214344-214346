# Frontend environment variables (CRA-compatible naming)
# These are read safely via src/utils/env.js and do not require Node's process polyfill at runtime.

# Base URL for backend API. If omitted, defaults to "/api".
# Examples:
#   http://localhost:8000
#   https://api.example.com
REACT_APP_API_BASE_URL=/api

# Enable demo auth flow (username/password) in the UI.
# Accepted truthy: "true", "1", "yes", "on"
# Accepted falsy:  "false", "0", "no", "off"
# Default is true if not set.
REACT_APP_DEMO_AUTH=true

# Runtime override note:
# In production you can also inject variables at runtime via a global:
#   <script>window.__ENV__ = { REACT_APP_API_BASE_URL: "https://api.example.com", REACT_APP_DEMO_AUTH: "false" };</script>
# The helper getEnv() will read window.__ENV__ first, then import.meta.env, then guarded process.env.

# Environment variables for Branding Compliance Frontend

# Base URL for backend API (no trailing slash)
# Example for local dev with proxy or same-origin:
REACT_APP_API_BASE_URL=/api

# Enable frontend-only demo authentication.
# When true, the Login screen supports username/password and accepts:
#   username: admin
#   password: password
# AuthContext will set:
#   user.id = 'admin'
#   roles = ['admin','approver','operator','viewer']
#   role = 'admin'
# The api client will emit X-User-ID: admin after login.
# IMPORTANT: Set to false in production when backend auth is available.
REACT_APP_DEMO_AUTH=true
